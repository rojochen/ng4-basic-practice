import { RanDomColorDirective } from './ran-dom-color.directive';

describe('RanDomColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RanDomColorDirective();
    expect(directive).toBeTruthy();
  });
});
