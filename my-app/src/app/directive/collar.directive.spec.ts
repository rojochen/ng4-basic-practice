import { CollarDirective } from './collar.directive';

describe('CollarDirective', () => {
  it('should create an instance', () => {
    const directive = new CollarDirective();
    expect(directive).toBeTruthy();
  });
});
