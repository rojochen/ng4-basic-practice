import { AppCollarDirective } from './app-collar.directive';

describe('AppCollarDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCollarDirective();
    expect(directive).toBeTruthy();
  });
});
