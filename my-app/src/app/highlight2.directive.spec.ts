import { Highlight2Directive } from './highlight2.directive';

describe('Highlight2Directive', () => {
  it('should create an instance', () => {
    const directive = new Highlight2Directive();
    expect(directive).toBeTruthy();
  });
});
